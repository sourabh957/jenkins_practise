pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello World" > file.txt
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'file.txt', 
            onlyIfSuccessful: true
        }
    }
}
