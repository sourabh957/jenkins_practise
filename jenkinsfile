pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello World" > file.txt'
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'file.txt', 
            onlyIfSuccessful: true
        }
    }
}
